package com.purabmodi.devhacksapp.api

interface SimpleApi {

    // here posts/1 is the end point of the url where the data is present in the website
//    @POST("/Students/users")
//    suspend fun sendDetails.(@Field("id")usn :String, @Field("group")group:String): Response<Void>
// userData is the name of our data class which we created in the first step
}